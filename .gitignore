# Local env / keys
.env
.env.local
*.key

# Per-contest keyword secrets — only the salted hash is committed (via `worker create`)
secrets/*
!secrets/example.json

# Node
node_modules/
dist/
.svelte-kit/
build/

# Tooling (local/personal)
.claude/settings.local.json
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
