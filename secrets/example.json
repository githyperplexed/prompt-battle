{
	"_comment": "Copy to secrets/<youtube-video-id>.json. 'keywords' = the 3 hidden words; 'salt' = a long random string. Only the salted hash is stored on the contest, so the words stay uncommitted yet verifiable after the reveal.",
	"keywords": ["EXAMPLE-ONE", "EXAMPLE-TWO", "EXAMPLE-THREE"],
	"salt": "replace-with-a-long-random-string"
}
